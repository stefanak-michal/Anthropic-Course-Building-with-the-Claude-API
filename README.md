# Anthropic course - Building with the Claude API

This repository contains code wrote while going through course.

I work with Windows without python installed, therefore I'm running everything via docker container.

_Code is written, not generated._